
default: &default
  adapter: sqlite3
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3

test:
  <<: *default
  database: pjt_firstSNS_test

# production server用
production:
  adapter: mysql2
  encoding: utf8mb4
  reconnect: false
  database: データベースの名前
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  username: root
  password:
  host: localhost
